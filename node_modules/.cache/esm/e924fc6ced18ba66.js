_ed1‍.x([["cli",()=>cli]]);var exec = require('child_process').exec;

       function cli(args){
  const fnArg = (JSON.stringify(args.splice(2)));

    if(fnArg==='["on"]'){
      exec('./yeelight-scene.sh 0 On');
      console.log("light On");
    }else if(fnArg==='["off"]'){
      exec('./yeelight-scene.sh 0 Off');
    }else if(args.splice(2).length === 0){
      console.log("use ylight on/off options");
    }

}
